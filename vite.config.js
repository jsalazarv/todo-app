/*
import { defineConfig } from 'vite'

export default defineConfig((config) => {
    return {
        assetsDir: 'assets',
        assetsInclude: ['assets/img/!*'],
        plugins: [],
        publicDir: 'dist',
        root: '.',
        build: {
            emptyOutDir: true,
            outDir: './dist'
        }
    }
});*/
